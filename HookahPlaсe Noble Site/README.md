# Hookah Place Noble — Site

Премиальный сайт для кальянного лаунжа Hookah Place Noble (Сочи).
Реализован как SPA на React, с акцентом на визуальный стиль, анимации и интеграцию с бронированием.

**URL**: [noble-hookahplace-sochi.ru](https://noble-hookahplace-sochi.ru) (пример)
**Статус**: Pre-deploy

---

## 🛠 Технический стек

### Core
- **Frontend**: React 18
- **Build Tool**: Vite 4
- **Language**: JavaScript (ESModules)
- **Styling**: Vanilla CSS (CSS Modules / Global Styles)

### Библиотеки
- **Анимации**: `framer-motion` (появление блоков, переходы)
- **Шейдеры**: `@paper-design/shaders-react` (жидкий фон мрамора)
- **Утилиты**: `react-input-mask` (маски телефонов), `emailjs` (отправка форм - legacy/backup)

---

## ✨ Функционал

1. **Age Gate**
   - Блокирующий экран "Вам есть 18?"
   - Сохранение выбора в `localStorage` (сессия).

2. **Бронирование**
   - Кастомная форма (Имя, Телефон, Дата/Время, Гости, Лаунж/Play зона).
   - Интеграция с Telegram Bot API (через Backend).
   - Валидация полей.

3. **Интерактив**
   - WebGL фон (Marble Shader) на первом экране.
   - Горизонтальный скролл галереи интерьеров.
   - Параллакс эффекты.

4. **Юридический блок**
   - Модальное окно "Правовая информация".
   - Соответствие ФЗ-152 (ПДн).
   - Маркировка Meta Platforms*.

---

## 🚀 Установка и запуск

### Предварительные требования
- Node.js 18+
- npm 9+

### 1. Установка зависимостей
```bash
npm install
```

### 2. Настройка окружения
Создайте файл `.env` в корне проекта (если нет):
```env
# URL бэкенда для API запросов (бронирование)
VITE_API_BASE=http://localhost:8000/api
```

### 3. Локальная разработка
Запускает dev-сервер на http://localhost:5173
```bash
npm run dev
```

### 4. Сборка (Production)
```bash
npm run build
```
Результат сборки будет в папке `dist/`.

---

## 🐳 Docker Deployment

Проект упаковывается в Nginx контейнер. Используется multi-stage build.

### Сборка образа
```bash
docker build -t noble-site .
```

### Запуск контейнера
```bash
docker run -d -p 80:80 noble-site
```
*Контейнер отдает статику через Nginx. Для проксирования API запросов требуется настройка внешнего Nginx или ingress.*

---

## 📂 Структура проекта

```
src/
├── components/      # React компоненты
│   ├── Hero/        # Первый экран (WebGL)
│   ├── Booking/     # Форма бронирования
│   ├── Gallery/     # Слайдер фото
│   ├── AgeGate/     # Проверка возраста
│   └── Footer/      # Подвал и контакты
├── assets/          # Изображения, иконки, видео
├── index.css        # Глобальные стили и переменные
├── main.jsx         # Точка входа React
└── App.jsx          # Корневой компонент
```

---

## ⚖️ Юридическая информация
*Meta Platforms Inc. признана экстремистской организацией и запрещена на территории РФ.
Сайт соблюдает требования законодательства РФ о рекламе табачной продукции (дисклеймеры 18+).
