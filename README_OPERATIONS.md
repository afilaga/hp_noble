# Hookah Place Noble — Инструкция по управлению сайтом

## 🚀 Как вносить изменения

Сайт настроен на **автоматический деплой**. Любые изменения, отправленные в GitHub, сразу попадают на сервер `hpnoble.ru`.

### 1. Обновление галереи (Интерьер)
Теперь не нужно менять код, чтобы добавить или удалить фото.
1. Перейдите в папку: `Site/HookahPlaсe Noble Site/src/assets/gallery/`
2. Добавьте новые изображения или удалите старые.
   * *Рекомендуемый формат: WebP или JPG (до 1MB для быстрой загрузки).*
3. Выполните в терминале:
   ```bash
   git add .
   git commit -m "Обновил фото в галерее"
   git push
   ```
4. Через 2-3 минуты сайт обновится автоматически.

### 2. Изменение контактов или текстов
* **Тексты и структура:** Основные компоненты находятся в `src/components/`.
* **Стили:** Глобальные правки (цвета, шрифты, отступы) — `src/index.css`.

---

## 🛠 Техническая информация

### Доступы
* **GitHub Репозиторий:** `https://github.com/afilaga/hp_noble.git`
* **Хостинг (Reg.ru):** `31.31.197.32` (u3370970)
* **API Бэкенда (Бронирование):** `https://api.hpnoble.ru/api`

### Структура проекта
* `src/assets/gallery/` — все фото интерьера.
* `src/components/ReservationForm.jsx` — логика бронирования.
* `.github/workflows/deploy_frontend.yml` — настройки авто-деплоя.

### Важные секреты (GitHub Secrets)
Для работы деплоя в настройках GitHub прописаны:
* `HOSTING_SERVER`, `HOSTING_USERNAME`, `HOSTING_PASSWORD`.

---

## ⚠️ Чек-лист при проблемах
1. **Сайт не обновился:** Проверьте вкладку **Actions** в GitHub. Если там красный крестик — значит сборка упала с ошибкой.
2. **SSL (HTTPS) не работает:** Убедитесь, что в панели ISPmanager на Reg.ru выпущен и подключен сертификат Let's Encrypt для домена `hpnoble.ru`.
3. **Бронирование не уходит:** Проверьте доступность бэкенда по адресу `http://178.20.211.174:8000/docs`.
