name: Deploy Backend to VPS

on:
  push:
    branches: [ main ]
    paths:
      - 'Booking/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Deploy via SSH
      uses: appleboy/ssh-action@master
      with:
        host: 178.20.211.174
        username: andrey
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        script: |
          bash /home/andrey/apps/noble-booking/Booking/deploy/deploy_vps.sh
